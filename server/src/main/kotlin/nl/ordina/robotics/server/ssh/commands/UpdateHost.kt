package nl.ordina.robotics.server.ssh.commands

// suspend fun Robot.updateHost(command: UpdateHost): Message {
//    sendMessage(Info("Updating host to ${command.host}..."))
//    settingsLock.withLock {
//        settings = settings.copy(host = command.host.trim()).also(SshSettingsLoader::save)
//    }
//
//    return Settings(settings)
// }
